<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.login.FinishCadastro"
    android:background="@drawable/gradient"
    android:gravity="center_horizontal">

    <ImageView
        android:layout_width="250dp"
        android:layout_height="23dp"
        android:layout_marginTop="40dp"
        android:src="@drawable/ic_logo"
        android:layout_centerHorizontal="true"/>

    <LinearLayout
        android:layout_width="400dp"
        android:layout_height="670dp"
        android:layout_marginTop="100dp"
        android:layout_marginRight="15dp"
        android:layout_marginLeft="15dp"
        android:background="@drawable/custom_cadastro"
        android:gravity="center_horizontal"
        android:orientation="vertical">


        <ImageView
            android:id="@+id/iv_background"
            android:layout_width="match_parent"
            android:layout_height="400sp"
            android:layout_marginTop="30dp"
            android:src="@drawable/success"/>

        <TextView
            android:id="@+id/tv_texto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="45dp"
            android:text="Sua conta na SearchDevelopers \n foi criada com sucesso!!"
            android:textColor="#CB636363"
            android:textSize="20dp"
            android:textStyle="bold"
            android:textAlignment="center"/>

        <Button
            android:id="@+id/bt_botao"
            android:layout_width="200dp"
            android:layout_height="40dp"
            android:layout_marginTop="70dp"
            android:background="@drawable/custom_button"
            android:onClick="rotaBotao"
            android:text="Entrar"
            android:textAllCaps="false"
            android:textColor="#ffffff"
            android:textSize="18sp" />

        <Button
            android:layout_width="100dp"
            android:layout_height="30dp"
            android:onClick="verificarCadastro"
            android:text="111111" />
    </LinearLayout>

</RelativeLayout>